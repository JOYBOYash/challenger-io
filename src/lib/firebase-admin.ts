// Firebase Admin SDK is optional - only used if service account is configured
// For now, we rely on the client SDK with proper Firestore security rules
// To enable admin SDK in the future, set FIREBASE_ADMIN_SDK_KEY environment variable

export function getFirestoreInstance() {
  return null;
}

export function getAuthInstance() {
  return null;
}

